"""Autogenerated OpenBB /equity/calendar Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
import datetime
from typing import Annotated, Union, Optional, Literal
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField


class ROUTER_equity_calendar(Container):
    """/equity/calendar
    dividend
    earnings
    events
    ipo
    splits
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def dividend(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(description="Start date of the data, in YYYY-MM-DD format."),
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None, OpenBBField(description="End date of the data, in YYYY-MM-DD format.")
        ] = None,
        provider: Annotated[
            Optional[Literal["fmp", "nasdaq"]],
            OpenBBField(
                description=(
                    "The provider to use, by default None. If None, the"
                    "priority list configured in the settings is used."
                    "Default priority: fmp, nasdaq."
                )
            ),
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get historical and upcoming dividend payments. Includes dividend amount, ex-dividend and payment dates.

        Parameters
        ----------
        provider : str
            The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fmp, nasdaq.
        start_date : date | str | None
            Start date of the data, in YYYY-MM-DD format.
        end_date : date | str | None
            End date of the data, in YYYY-MM-DD format.

        Returns
        -------
        OBBject
            results : list[CalendarDividend]
                Serializable results.
            provider : Optional[str]
                Provider name.
            warnings : Optional[list[Warning_]]
                list of warnings.
            chart : Optional[Chart]
                Chart object.
            extra : dict[str, Any]
                Extra info.

        CalendarDividend
        ----------------
        ex_dividend_date : date
            The ex-dividend date - the date on which the stock begins trading without rights to the dividend.
        symbol : str
            Symbol representing the entity requested in the data.
        amount : float | None
            The dividend amount per share.
        name : str | None
            Name of the entity.
        record_date : date | None
            The record date of ownership for eligibility.
        payment_date : date | None
            The payment date of the dividend.
        declaration_date : date | None
            Declaration date of the dividend.
        adjusted_amount : float | None
            The adjusted-dividend amount. (provider: fmp)
        dividend_yield : float | None
            Annualized dividend yield. (provider: fmp)
        frequency : str | None
            Frequency of the regular dividend payment. (provider: fmp)
        annualized_amount : float | None
            The indicated annualized dividend amount. (provider: nasdaq)

        Examples
        --------
        >>> from openbb import obb
        >>> obb.equity.calendar.dividend()
        >>> # Get dividend calendar for specific dates.
        >>> obb.equity.calendar.dividend(start_date='2024-02-01', end_date='2024-02-07')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/equity/calendar/dividend",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "equity.calendar.dividend",
                        ("fmp", "nasdaq"),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def earnings(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(description="Start date of the data, in YYYY-MM-DD format."),
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None, OpenBBField(description="End date of the data, in YYYY-MM-DD format.")
        ] = None,
        provider: Annotated[
            Optional[Literal["fmp", "nasdaq", "seeking_alpha", "tmx"]],
            OpenBBField(
                description=(
                    "The provider to use, by default None. If None, the"
                    "priority list configured in the settings is used."
                    "Default priority: fmp, nasdaq, seeking_alpha, tmx."
                )
            ),
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get historical and upcoming company earnings releases. Includes earnings per share (EPS) and revenue data.

        Parameters
        ----------
        provider : str
            The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fmp, nasdaq, seeking_alpha, tmx.
        start_date : date | str | None
            Start date of the data, in YYYY-MM-DD format.
        end_date : date | str | None
            End date of the data, in YYYY-MM-DD format.
        country : str
            The country to get calendar data for. Accepts 'us'/'ca', ISO codes ('US', 'USA', 'CA', 'CAN'), or names ('United States', 'Canada'). (provider: seeking_alpha)
            Choices for seeking_alpha: 'us', 'ca'

        Returns
        -------
        OBBject
            results : list[CalendarEarnings]
                Serializable results.
            provider : Optional[str]
                Provider name.
            warnings : Optional[list[Warning_]]
                list of warnings.
            chart : Optional[Chart]
                Chart object.
            extra : dict[str, Any]
                Extra info.

        CalendarEarnings
        ----------------
        report_date : date
            The date of the earnings report.
        symbol : str
            Symbol representing the entity requested in the data.
        name : str | None
            Name of the entity.
        eps_previous : float | None
            The earnings-per-share from the same previously reported period.
        eps_consensus : float | None
            The analyst conesus earnings-per-share estimate.
        eps_actual : float | None
            The actual earnings per share announced. (provider: fmp, nasdaq);
            The actual EPS in dollars. (provider: tmx)
        revenue_consensus : float | None
            The revenue forecast consensus. (provider: fmp)
        revenue_actual : float | None
            The actual reported revenue. (provider: fmp)
        last_updated : date | None
            The date the data was updated last. (provider: fmp)
        surprise_percent : float | None
            The earnings surprise as normalized percentage points. (provider: nasdaq);
            The EPS surprise as a normalized percent. (provider: tmx)
        num_estimates : int | None
            The number of analysts providing estimates for the consensus. (provider: nasdaq)
        period_ending : str | None
            The fiscal period end date. (provider: nasdaq)
        previous_report_date : date | None
            The previous report date for the same period last year. (provider: nasdaq)
        reporting_time : str | None
            The reporting time - e.g. after market close. (provider: nasdaq, seeking_alpha);
            The time of the report - i.e., before or after market. (provider: tmx)
        market_cap : float | int | None
            The market cap (USD) of the reporting entity. (provider: nasdaq);
            Market cap of the entity. (provider: seeking_alpha)
        exchange : str | None
            The primary trading exchange. (provider: seeking_alpha)
        sector_id : int | None
            The Seeking Alpha Sector ID. (provider: seeking_alpha)
        eps_surprise : float | None
            The EPS surprise in dollars. (provider: tmx)

        Examples
        --------
        >>> from openbb import obb
        >>> obb.equity.calendar.earnings()
        >>> # Get earnings calendar for specific dates.
        >>> obb.equity.calendar.earnings(start_date='2024-02-01', end_date='2024-02-07')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/equity/calendar/earnings",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "equity.calendar.earnings",
                        ("fmp", "nasdaq", "seeking_alpha", "tmx"),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={"country": {"seeking_alpha": {"choices": ["us", "ca"]}}},
            )
        )

    @exception_handler
    @validate
    def events(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(description="Start date of the data, in YYYY-MM-DD format."),
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None, OpenBBField(description="End date of the data, in YYYY-MM-DD format.")
        ] = None,
        provider: Annotated[
            Optional[Literal["fmp"]],
            OpenBBField(
                description=(
                    "The provider to use, by default None. If None, the"
                    "priority list configured in the settings is used."
                    "Default priority: fmp."
                )
            ),
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get historical and upcoming company events, such as Investor Day, Conference Call, Earnings Release.

        Parameters
        ----------
        provider : str
            The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fmp.
        start_date : date | str | None
            Start date of the data, in YYYY-MM-DD format.
        end_date : date | str | None
            End date of the data, in YYYY-MM-DD format.

        Returns
        -------
        OBBject
            results : list[CalendarEvents]
                Serializable results.
            provider : Optional[str]
                Provider name.
            warnings : Optional[list[Warning_]]
                list of warnings.
            chart : Optional[Chart]
                Chart object.
            extra : dict[str, Any]
                Extra info.

        CalendarEvents
        --------------
        date : date
            The date of the data. The date of the event.
        symbol : str
            Symbol representing the entity requested in the data.
        exchange : str | None
            Exchange where the symbol is listed. (provider: fmp)
        time : str | None
            The estimated time of the event, local to the exchange. (provider: fmp)
        timing : str | None
            The timing of the event - e.g. before, during, or after market hours. (provider: fmp)
        description : str | None
            The title of the event. (provider: fmp)
        url : str | None
            The URL to the press release for the announcement. (provider: fmp)
        announcement_date : date | None
            The date when the event was announced. (provider: fmp)

        Examples
        --------
        >>> from openbb import obb
        >>> obb.equity.calendar.events()
        >>> # Get company events calendar for specific dates.
        >>> obb.equity.calendar.events(start_date='2024-02-01', end_date='2024-02-07')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/equity/calendar/events",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "equity.calendar.events",
                        ("fmp",),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def ipo(
        self,
        symbol: Annotated[str | None, OpenBBField(description="Symbol to get data for.")] = None,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(description="Start date of the data, in YYYY-MM-DD format."),
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None, OpenBBField(description="End date of the data, in YYYY-MM-DD format.")
        ] = None,
        limit: Annotated[int | None, OpenBBField(description="The number of data entries to return.")] = 100,
        provider: Annotated[
            Optional[Literal["fmp", "nasdaq"]],
            OpenBBField(
                description=(
                    "The provider to use, by default None. If None, the"
                    "priority list configured in the settings is used."
                    "Default priority: fmp, nasdaq."
                )
            ),
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get historical and upcoming initial public offerings (IPOs).

        Parameters
        ----------
        provider : str
            The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fmp, nasdaq.
        symbol : str | None
            Symbol to get data for.
        start_date : date | str | None
            Start date of the data, in YYYY-MM-DD format.
        end_date : date | str | None
            End date of the data, in YYYY-MM-DD format.
        limit : int | None
            The number of data entries to return.
        status : str
            The status of the IPO. (provider: nasdaq)
            Choices for nasdaq: 'upcoming', 'priced', 'filed', 'withdrawn'
        is_spo : bool
            If True, returns data for secondary public offerings (SPOs). (provider: nasdaq)

        Returns
        -------
        OBBject
            results : list[CalendarIpo]
                Serializable results.
            provider : Optional[str]
                Provider name.
            warnings : Optional[list[Warning_]]
                list of warnings.
            chart : Optional[Chart]
                Chart object.
            extra : dict[str, Any]
                Extra info.

        CalendarIpo
        -----------
        symbol : str | None
            Symbol representing the entity requested in the data.
        ipo_date : date | None
            The date of the IPO, when the stock first trades on a major exchange.
        exchange_date : datetime | None
            Timezone information for the exchange and date of the IPO. (provider: fmp)
        name : str | None
            The name of the entity going public. (provider: fmp);
            The name of the company. (provider: nasdaq)
        exchange : str | None
            The exchange where the IPO is listed. (provider: fmp)
        actions : str | None
            Actions related to the IPO, such as, Expected, Priced, Filed, Amended. (provider: fmp)
        shares : int | float | None
            The number of shares being offered in the IPO. (provider: fmp)
        price_range : str | None
            The expected price range for the IPO shares. (provider: fmp)
        market_cap : int | float | None
            The estimated market capitalization of the company at the time of the IPO. (provider: fmp)
        offer_amount : float | None
            The dollar value of the shares offered. (provider: nasdaq)
        share_count : int | None
            The number of shares offered. (provider: nasdaq)
        expected_price_date : date | None
            The date the pricing is expected. (provider: nasdaq)
        filed_date : date | None
            The date the IPO was filed. (provider: nasdaq)
        withdraw_date : date | None
            The date the IPO was withdrawn. (provider: nasdaq)
        deal_status : str | None
            The status of the deal. (provider: nasdaq)

        Examples
        --------
        >>> from openbb import obb
        >>> obb.equity.calendar.ipo()
        >>> obb.equity.calendar.ipo(limit=100)
        >>> # Get all IPOs available.
        >>> obb.equity.calendar.ipo()
        >>> # Get IPOs for specific dates.
        >>> obb.equity.calendar.ipo(start_date='2024-02-01', end_date='2024-02-07')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/equity/calendar/ipo",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "equity.calendar.ipo",
                        ("fmp", "nasdaq"),
                    )
                },
                standard_params={
                    "symbol": symbol,
                    "start_date": start_date,
                    "end_date": end_date,
                    "limit": limit,
                },
                extra_params=kwargs,
                info={"status": {"nasdaq": {"choices": ["upcoming", "priced", "filed", "withdrawn"]}}},
            )
        )

    @exception_handler
    @validate
    def splits(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(description="Start date of the data, in YYYY-MM-DD format."),
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None, OpenBBField(description="End date of the data, in YYYY-MM-DD format.")
        ] = None,
        provider: Annotated[
            Optional[Literal["fmp"]],
            OpenBBField(
                description=(
                    "The provider to use, by default None. If None, the"
                    "priority list configured in the settings is used."
                    "Default priority: fmp."
                )
            ),
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get historical and upcoming stock split operations.

        Parameters
        ----------
        provider : str
            The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fmp.
        start_date : date | str | None
            Start date of the data, in YYYY-MM-DD format.
        end_date : date | str | None
            End date of the data, in YYYY-MM-DD format.

        Returns
        -------
        OBBject
            results : list[CalendarSplits]
                Serializable results.
            provider : Optional[str]
                Provider name.
            warnings : Optional[list[Warning_]]
                list of warnings.
            chart : Optional[Chart]
                Chart object.
            extra : dict[str, Any]
                Extra info.

        CalendarSplits
        --------------
        date : date
            The date of the data.
        symbol : str
            Symbol representing the entity requested in the data.
        numerator : float
            Numerator of the stock split.
        denominator : float
            Denominator of the stock split.

        Examples
        --------
        >>> from openbb import obb
        >>> obb.equity.calendar.splits()
        >>> # Get stock splits calendar for specific dates.
        >>> obb.equity.calendar.splits(start_date='2024-02-01', end_date='2024-02-07')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/equity/calendar/splits",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "equity.calendar.splits",
                        ("fmp",),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
            )
        )
