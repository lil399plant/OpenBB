"""Autogenerated OpenBB /news Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
import datetime
from typing import Annotated, Union, Optional, Literal
from annotated_types import Ge
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField


class ROUTER_news(Container):
    """/news
    company
    world
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def company(
        self,
        symbol: Annotated[
            Union[str, None, list[str | None]],
            OpenBBField(
                description=(
                    "Symbol to get data for. Multiple comma separated"
                    "items allowed for provider(s): benzinga, fmp, tmx,"
                    "yfinance."
                )
            ),
        ] = None,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(description="Start date of the data, in YYYY-MM-DD format."),
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None, OpenBBField(description="End date of the data, in YYYY-MM-DD format.")
        ] = None,
        limit: Annotated[
            Optional[Annotated[int, Ge(ge=0)]], OpenBBField(description="The number of data entries to return.")
        ] = None,
        provider: Annotated[
            Optional[Literal["benzinga", "fmp", "tmx", "yfinance"]],
            OpenBBField(
                description=(
                    "The provider to use, by default None. If None, the"
                    "priority list configured in the settings is used."
                    "Default priority: benzinga, fmp, tmx, yfinance."
                )
            ),
        ] = None,
        **kwargs
    ) -> OBBject:
        """Company News. Get news for one or more companies.

        Parameters
        ----------
        provider : str
            The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: benzinga, fmp, tmx, yfinance.
        symbol : str | list[str | None] | None
            Symbol to get data for. Multiple comma separated items allowed for provider(s): benzinga, fmp, tmx, yfinance.
        start_date : date | str | None
            Start date of the data, in YYYY-MM-DD format.
        end_date : date | str | None
            End date of the data, in YYYY-MM-DD format.
        limit : Annotated[int, Ge(ge=0) | None]
            The number of data entries to return.
        date : date | None
            A specific date to get data for. (provider: benzinga)
        display : str
            Specify headline only (headline), headline + teaser (abstract), or headline + full body (full). (provider: benzinga)
            Choices for benzinga: 'headline', 'abstract', 'full'
        updated_since : int | None
            Number of seconds since the news was updated. (provider: benzinga)
        published_since : int | None
            Number of seconds since the news was published. (provider: benzinga)
        sort : str
            Key to sort the news by. (provider: benzinga)
            Choices for benzinga: 'id', 'created', 'updated'
        order : str
            Order to sort the news by. (provider: benzinga)
            Choices for benzinga: 'asc', 'desc'
        isin : str | None
            The company's ISIN. (provider: benzinga)
        cusip : str | None
            The company's CUSIP. (provider: benzinga)
        channels : str | None
            Channels of the news to retrieve. (provider: benzinga)
        topics : str | None
            Topics of the news to retrieve. (provider: benzinga)
        authors : str | None
            Authors of the news to retrieve. (provider: benzinga)
        content_types : str | None
            Content types of the news to retrieve. (provider: benzinga)
        page : int | None
            (provider: fmp)
                Page number of the results. Use in combination with limit.

            (provider: tmx)
                The page number to start from. Use with limit.
        press_release : bool | None
            When true, will return only press releases for the given symbol(s). (provider: fmp)

        Returns
        -------
        OBBject
            results : list[CompanyNews]
                Serializable results.
            provider : Optional[str]
                Provider name.
            warnings : Optional[list[Warning_]]
                list of warnings.
            chart : Optional[Chart]
                Chart object.
            extra : dict[str, Any]
                Extra info.

        CompanyNews
        -----------
        date : datetime
            The date of the data. The date of publication.
        title : str
            Title of the article.
        author : str | None
            Author of the article.
        excerpt : str | None
            Excerpt of the article text.
        body : str | None
            Body of the article text.
        images : Any | None
            Images associated with the article.
        url : str
            URL to the article.
        symbols : str | None
            Symbols associated with the article.
        channels : str | None
            Channels associated with the news. (provider: benzinga)
        tags : str | None
            Tags associated with the news. (provider: benzinga)
        updated : datetime | None
            Updated date of the news. (provider: benzinga)
        id : str | None
            Article ID. (provider: benzinga)
        original_id : str | None
            Original ID of the news article. (provider: benzinga)
        source : str | None
            Name of the news site. (provider: fmp);
            Source of the news. (provider: tmx);
            Source of the news article (provider: yfinance)

        Examples
        --------
        >>> from openbb import obb
        >>> obb.news.company()
        >>> obb.news.company(limit=100)
        >>> # Get news on the specified dates.
        >>> obb.news.company(symbol='AAPL', start_date='2024-02-01', end_date='2024-02-07')
        >>> # Display the headlines of the news.
        >>> obb.news.company(symbol='AAPL', display='headline')
        >>> # Get news for multiple symbols.
        >>> obb.news.company(symbol='aapl,tsla')
        >>> # Get news company's ISIN.
        >>> obb.news.company(symbol='NVDA', isin='US0378331005')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/news/company",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "news.company",
                        ("benzinga", "fmp", "tmx", "yfinance"),
                    )
                },
                standard_params={
                    "symbol": symbol,
                    "start_date": start_date,
                    "end_date": end_date,
                    "limit": limit,
                },
                extra_params=kwargs,
                info={
                    "symbol": {
                        "benzinga": {"multiple_items_allowed": True, "choices": None},
                        "fmp": {"multiple_items_allowed": True, "choices": None},
                        "tmx": {"multiple_items_allowed": True, "choices": None},
                        "yfinance": {"multiple_items_allowed": True, "choices": None},
                    },
                    "display": {"benzinga": {"choices": ["headline", "abstract", "full"]}},
                    "sort": {"benzinga": {"choices": ["id", "created", "updated"]}},
                    "order": {"benzinga": {"choices": ["asc", "desc"]}},
                },
            )
        )

    @exception_handler
    @validate
    def world(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(description=("Start date of the data, in YYYY-MM-DD format. The" "default is 2 weeks ago.")),
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(description=("End date of the data, in YYYY-MM-DD format. The" "default is today.")),
        ] = None,
        limit: Annotated[
            Optional[Annotated[int, Ge(ge=0)]],
            OpenBBField(description=("The number of data entries to return. The number" "of articles to return.")),
        ] = None,
        provider: Annotated[
            Optional[Literal["benzinga", "fmp"]],
            OpenBBField(
                description=(
                    "The provider to use, by default None. If None, the"
                    "priority list configured in the settings is used."
                    "Default priority: benzinga, fmp."
                )
            ),
        ] = None,
        **kwargs
    ) -> OBBject:
        """World News. Global news data.

        Parameters
        ----------
        provider : str
            The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: benzinga, fmp.
        start_date : date | str | None
            Start date of the data, in YYYY-MM-DD format. The default is 2 weeks ago.
        end_date : date | str | None
            End date of the data, in YYYY-MM-DD format. The default is today.
        limit : Annotated[int, Ge(ge=0) | None]
            The number of data entries to return. The number of articles to return.
        date : date | None
            A specific date to get data for. (provider: benzinga)
        display : str
            Specify headline only (headline), headline + teaser (abstract), or headline + full body (full). (provider: benzinga)
            Choices for benzinga: 'headline', 'abstract', 'full'
        updated_since : int | None
            Number of seconds since the news was updated. (provider: benzinga)
        published_since : int | None
            Number of seconds since the news was published. (provider: benzinga)
        sort : str
            Key to sort the news by. (provider: benzinga)
            Choices for benzinga: 'id', 'created', 'updated'
        order : str
            Order to sort the news by. (provider: benzinga)
            Choices for benzinga: 'asc', 'desc'
        isin : str | None
            The ISIN of the news to retrieve. (provider: benzinga)
        cusip : str | None
            The CUSIP of the news to retrieve. (provider: benzinga)
        channels : str | None
            Channels of the news to retrieve. (provider: benzinga)
        topics : str | None
            Topics of the news to retrieve. (provider: benzinga)
        authors : str | None
            Authors of the news to retrieve. (provider: benzinga)
        content_types : str | None
            Content types of the news to retrieve. (provider: benzinga)
        topic : str
            The topic of the news to be fetched. (provider: fmp)
            Choices for fmp: 'fmp_articles', 'general', 'press_releases', 'stocks', 'forex', 'crypto'
        page : int | None
            Page number of the results. Use in combination with limit. (provider: fmp)

        Returns
        -------
        OBBject
            results : list[WorldNews]
                Serializable results.
            provider : Optional[str]
                Provider name.
            warnings : Optional[list[Warning_]]
                list of warnings.
            chart : Optional[Chart]
                Chart object.
            extra : dict[str, Any]
                Extra info.

        WorldNews
        ---------
        date : datetime
            The date of the data. The date of publication.
        title : str
            Title of the article.
        author : str | None
            Author of the article.
        excerpt : str | None
            Excerpt of the article text.
        body : str | None
            Body of the article text.
        images : Any | None
            Images associated with the article.
        url : str | None
            URL to the article.
        channels : str | None
            Channels associated with the news. (provider: benzinga)
        stocks : str | None
            Stocks associated with the news. (provider: benzinga)
        tags : str | None
            Tags associated with the news. (provider: benzinga)
        updated : datetime | None
            Updated date of the news. (provider: benzinga)
        id : str | None
            Article ID. (provider: benzinga)
        updated_id : str | None
            Updated article ID if the article was updated. (provider: benzinga)
        source : str | None
            News source. (provider: fmp)

        Examples
        --------
        >>> from openbb import obb
        >>> obb.news.world()
        >>> obb.news.world(limit=100)
        >>> # Get news on the specified dates.
        >>> obb.news.world(start_date='2024-02-01', end_date='2024-02-07')
        >>> # Display the headlines of the news.
        >>> obb.news.world(display='headline')
        >>> # Get news by topics.
        >>> obb.news.world(topics='finance')
        >>> # Get news by source using 'tingo' as provider.
        >>> obb.news.world(source='bloomberg')
        >>> # Filter aticles by term using 'biztoc' as provider.
        >>> obb.news.world(term='apple')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/news/world",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "news.world",
                        ("benzinga", "fmp"),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                    "limit": limit,
                },
                extra_params=kwargs,
                info={
                    "display": {"benzinga": {"choices": ["headline", "abstract", "full"]}},
                    "sort": {"benzinga": {"choices": ["id", "created", "updated"]}},
                    "order": {"benzinga": {"choices": ["asc", "desc"]}},
                    "topic": {
                        "fmp": {"choices": ["fmp_articles", "general", "press_releases", "stocks", "forex", "crypto"]}
                    },
                },
            )
        )
