"""Autogenerated OpenBB /index/price Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
import datetime
from typing import Annotated, Union, Optional, Literal
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField


class ROUTER_index_price(Container):
    """/index/price
    historical
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def historical(
        self,
        symbol: Annotated[
            Union[str, list[str]],
            OpenBBField(
                description=(
                    "Symbol to get data for. Multiple comma separated"
                    "items allowed for provider(s): cboe, fmp,"
                    "yfinance."
                )
            ),
        ],
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(description="Start date of the data, in YYYY-MM-DD format."),
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None, OpenBBField(description="End date of the data, in YYYY-MM-DD format.")
        ] = None,
        chart: Annotated[
            bool, OpenBBField(description=("Whether to create a chart or not, by default" "False."))
        ] = False,
        provider: Annotated[
            Optional[Literal["cboe", "fmp", "yfinance"]],
            OpenBBField(
                description=(
                    "The provider to use, by default None. If None, the"
                    "priority list configured in the settings is used."
                    "Default priority: cboe, fmp, yfinance."
                )
            ),
        ] = None,
        **kwargs
    ) -> OBBject:
        """Historical Index Levels.

        Parameters
        ----------
        provider : str
            The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: cboe, fmp, yfinance.
        symbol : str | list[str]
            Symbol to get data for. Multiple comma separated items allowed for provider(s): cboe, fmp, yfinance.
        start_date : date | str | None
            Start date of the data, in YYYY-MM-DD format.
        end_date : date | str | None
            End date of the data, in YYYY-MM-DD format.
        interval : str
            Time interval of the data to return.

            (provider: cboe)
                The most recent trading day is not including in daily historical data. Intraday data is only available for the most recent trading day at 1 minute intervals.
                Choices: '1m', '1d'
        use_cache : bool
            When True, the company directories will be cached for 24 hours and are used to validate symbols. The results of the function are not cached. Set as False to bypass. (provider: cboe)
        chart : bool
            Whether to create a chart or not, by default False.

        Returns
        -------
        OBBject
            results : list[IndexHistorical]
                Serializable results.
            provider : Optional[str]
                Provider name.
            warnings : Optional[list[Warning_]]
                list of warnings.
            chart : Optional[Chart]
                Chart object.
            extra : dict[str, Any]
                Extra info.

        IndexHistorical
        ---------------
        symbol : str | None
            Symbol representing the entity requested in the data.
        date : date | datetime
            The date of the data.
        open : float | None
            The open price.
        high : float | None
            The high price.
        low : float | None
            The low price.
        close : float | None
            The close price.
        volume : int | float | None
            The trading volume.
        calls_volume : float | None
            Number of calls traded during the most recent trading period. Only valid if interval is 1m. (provider: cboe)
        puts_volume : float | None
            Number of puts traded during the most recent trading period. Only valid if interval is 1m. (provider: cboe)
        total_options_volume : float | None
            Total number of options traded during the most recent trading period. Only valid if interval is 1m. (provider: cboe)
        vwap : float | None
            Volume Weighted Average Price over the period. (provider: fmp)
        change : float | None
            Change in the price from the previous close. (provider: fmp)
        change_percent : float | None
            Change in the price from the previous close, as a normalized percent. (provider: fmp)

        Examples
        --------
        >>> from openbb import obb
        >>> obb.index.price.historical(symbol='^GSPC')
        >>> # Not all providers have the same symbols.
        >>> obb.index.price.historical(symbol='SPX')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/index/price/historical",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "index.price.historical",
                        ("cboe", "fmp", "yfinance"),
                    )
                },
                standard_params={
                    "symbol": symbol,
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                chart=chart,
                info={
                    "symbol": {
                        "cboe": {"multiple_items_allowed": True, "choices": None},
                        "fmp": {"multiple_items_allowed": True, "choices": None},
                        "yfinance": {"multiple_items_allowed": True, "choices": None},
                    },
                    "interval": {
                        "cboe": {"multiple_items_allowed": False, "choices": ["1m", "1d"]},
                        "fmp": {"choices": ["1m", "5m", "1h", "1d"]},
                        "yfinance": {
                            "multiple_items_allowed": False,
                            "choices": ["1m", "2m", "5m", "15m", "30m", "60m", "90m", "1h", "1d", "5d", "1W", "1M", "1Q"],
                        },
                    },
                },
            )
        )
